<?php
/**
 * SHARED config — applies to every folder this script is symlinked into.
 * Put only truly global defaults here; keep it empty otherwise. Folder-specific
 * settings (title, noun, …) belong in a config.php INSIDE that content folder.
 *
 * Example:
 *   return ['default_view' => 'list'];
 */
return [];
